<Window x:Class="Listly.Views.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="clr-namespace:Listly.Behaviors"
        Title="M2_APEX"
        Width="720"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True">

    <Window.Resources>
        <b:KindToLabelConverter x:Key="KindToLabel" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <SolidColorBrush x:Key="CardBrush" Color="#F5121A28" />
        <SolidColorBrush x:Key="BorderBrush" Color="#FF2A3A4D" />
        <SolidColorBrush x:Key="TextBrush" Color="#CFE3F2" />
        <SolidColorBrush x:Key="SubtleBrush" Color="#8AA0B6" />
        <SolidColorBrush x:Key="AccentBrush" Color="#36D6FF" />
        <SolidColorBrush x:Key="SelBrush" Color="#FF1D3B57" />
        <SolidColorBrush x:Key="HoverBrush" Color="#FF15212F" />
        <SolidColorBrush x:Key="SeparatorBrush" Color="#FF1E2A3A" />

        <Style x:Key="ResultItemStyle" TargetType="ListBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                CornerRadius="8"
                                Background="Transparent"
                                Padding="10,7"
                                Margin="8,1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SelBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="18"
            CornerRadius="14"
            Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Opacity="0.55" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search box -->
            <Grid Grid.Row="0" Margin="18,14,18,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0"
                       Source="{DynamicResource M2LogoImage}"
                       Width="24"
                       Height="24"
                       Margin="0,0,12,0"
                       VerticalAlignment="Center"
                       Stretch="Uniform" />

                <Grid Grid.Column="1">
                    <TextBlock Text="Search files, apps, commands, and the web…"
                               VerticalAlignment="Center"
                               FontSize="20"
                               Foreground="{StaticResource SubtleBrush}"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=QueryBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBox x:Name="QueryBox"
                             Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{StaticResource TextBrush}"
                             CaretBrush="{StaticResource AccentBrush}"
                             FontSize="20"
                             VerticalContentAlignment="Center"
                             Padding="0"
                             SelectionBrush="{StaticResource AccentBrush}" />
                </Grid>
            </Grid>

            <!-- Separator (only when there are results) -->
            <Border Grid.Row="1"
                    Height="1"
                    Background="{StaticResource SeparatorBrush}"
                    Visibility="{Binding HasItems, ElementName=ResultsList, Converter={StaticResource BoolToVis}}" />

            <!-- Results -->
            <ListBox x:Name="ResultsList"
                     Grid.Row="2"
                     ItemsSource="{Binding Results}"
                     SelectedIndex="{Binding SelectedIndex}"
                     Background="Transparent"
                     BorderThickness="0"
                     MaxHeight="460"
                     Focusable="False"
                     Padding="0,6"
                     ItemContainerStyle="{StaticResource ResultItemStyle}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       FontFamily="Segoe MDL2 Assets"
                                       Text="{Binding Glyph}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource AccentBrush}" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock b:Highlight.Result="{Binding}"
                                           FontSize="15"
                                           Foreground="{StaticResource TextBrush}"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding Subtitle}"
                                           FontSize="11.5"
                                           Foreground="{StaticResource SubtleBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,1,0,0" />
                            </StackPanel>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding Kind, Converter={StaticResource KindToLabel}}"
                                       FontSize="10.5"
                                       VerticalAlignment="Center"
                                       Margin="10,0,4,0"
                                       Foreground="{StaticResource SubtleBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Status / hint bar -->
            <Border Grid.Row="3"
                    Background="#14000000"
                    CornerRadius="0,0,14,14">
                <Grid Margin="16,7">
                    <TextBlock Text="{Binding Status}"
                               FontSize="11"
                               Foreground="{StaticResource SubtleBrush}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock HorizontalAlignment="Right"
                               FontSize="11"
                               Foreground="{StaticResource SubtleBrush}"
                               Text="&#x2191;&#x2193; navigate &#160;&#160;&#x21B5; open &#160;&#160;Ctrl+&#x21B5; folder &#160;&#160;Esc close" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
